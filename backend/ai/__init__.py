"""AI provider package for the AI Review Engine.

Usage
-----
    from backend.ai.providers import get_provider
    provider = get_provider(app.config)
    result = provider.run_review(input_text, review_type, context)
"""
